#librería numpy 

import numpy as np 
from numpy import matrix
from pylab import *

print (np.arange(15)) #genero un lista de los primeros 15 números : 0 a 14
print ('---------------------------------------')

r=2
i=np.arange(n)
j=np.arange(n)
n=5
a = np.zeros(n*n).reshape(n,n) # acomodo los valores en una matriz de 3 filas por 5 columnas 

while r**2>=((i-n/2)**2):
   a = np.ones()
    
print (a)


imshow(a)
